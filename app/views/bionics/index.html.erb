<%= render partial:'header'%>
        <main  class="px-3">
            <h1>Bionic Articles</h1>   
                <p class = "lead ">
                    Did you know that your brain reads faster than your eye?
                </p>    
                <% @bionic.each do |bionic|  %> 
                    <ul class="btn btn-lg btn-secondary fw-bold border-white bg-white" > 
                       <%=  link_to "text_#{bionic.id}", show_bionic_path(id: bionic)%>
                    </ul>
                <%end%>
        </main>
<%= render partial:'footer'%>