<%= render partial:'header'%>

    <main  class="px-3 reading mt-5 text border ">
        <div>
            <h1>Text</h1>
            <p class = "lead_1"><%= @bionics.text_initial %></p>
        </div>
        <br />
        <div>
            <h1>Text Converted</h1>
                <p class = "lead_1 "><%= @bionics.text_converted.html_safe %></p>
        </div>
    </main>
        <button class="btn btn-lg  fw-bold border-white bg-white mt-2"> 
            <%= link_to 'Edit', edit_bionic_path(id:@bionics.id)%>
        </button>
        <button class="btn btn-lg  fw-bold border-white bg-white mt-2">
        <%= link_to 'Delete', bionic_path(@bionics), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
    </button>
<%= render partial:'footer'%>